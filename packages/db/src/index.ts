// Browser-safe exports only — import from "@juniorcode/db/server" for server-only clients
export { getSupabaseBrowserClient } from "./client.browser";
export type { Database } from "./database.types";
